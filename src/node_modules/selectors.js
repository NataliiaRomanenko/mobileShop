import * as R from 'ramda'

export const getPhoneById = (state, id) => state.phones[id];

export const getPhones = state => {
    const phones = R.map(id => getPhoneById(state,id), state.phonePage.ids)
    return phones
};

export const getRenderedPhonesLength = state => state.phonePage.ids.length;


